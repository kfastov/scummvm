# Проверка результата MCI и новый барьер Windows directory

## Дельта после checkpoint 0050 — 13 августа 2026

После `ledger/0050` сделано только следующее.

Машинно подтверждён builtin 70: таблица ведёт в `MTB40RUN` seg91:09bc,
ABI `W -> W`, `retf 2`. Реализация сравнивает unsigned input с 1 и возвращает
каноническое слово `1`, если input >= 1, иначе `0`. В стартовом `firstIdle` он
проверяет возвращённый `myMCITest` результат после materialize type `0x80`.
Нормальная ветка MCI возвращает 1, поэтому проверка проходит.

Добавлена только эта достигнутая семантика builtin 70. Другие формы и
неиспользуемые операции VM не реализовывались.

Беззвучный headless smoke (`SDL_AUDIODRIVER=dummy`) после изменения прошёл
дальше и зафиксировал:

```
ToolBook: AddFontResource cbook2.fon -> 6
ToolBook: GlobalAlloc 256 bytes -> 2
ToolBook: native function GetWindowsDirectory пока не реализована @0x131b1c
```

Текущий следующий фактически достигнутый барьер — native
`GetWindowsDirectory` по адресу `0x131b1c`. Он намеренно не реализован в этом
checkpoint. Страница остаётся формальной начальной `begin`; игровых переходов,
заранее заданного маршрута или подмены роликов не добавлено.
