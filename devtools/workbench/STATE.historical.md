# Состояние работ (файл производный, пересобирается tools/brief.py)

## Где сейчас
* барьер: ToolBook: OpenScript opcode 37 @0x74e14 пока не реализован
* всего барьеров в прогоне: 1
* последняя страница: ToolBook: страница 8/209 «mainMenu», фон 2, объектов 8, обработчиков 2, строк текста 0
* сборка от 2026-08-19 20:05
* ветка main, последний коммит: Живые документы догнали работу: ловушка стенда, состояние и карта инструментов
* незакоммиченное:
  * M .gitignore
  * M CLAUDE.md
  * M LEDGER.md
  * M NOTES.md
  * M patches/scummvm-director.patch
  * M tools/ne16.py
  * M tools/qmon.py
  * ?? STATE.md

## Последние записи леджера
* 2026-08-17 — 0082 — **Массивы растут на запись** (`seg60:0x11ce`, ветка только для записи), чтение за концом — пусто; 252 — `ValueArraySet` на 4 байта; `0x71` форма `0x25`; принята модель «арифметика пишет в отведённое `0x0f`/`0x59` место». `TestScores` пройден, книга дошла до страницы «BigBlack» ✅
* 2026-08-17 — 0083 — **147 — третья форма записи свойства** (у чтения `retf 6`, у записи `retf 0xa`; помощник `RUN81:0x1386` — цикл по набору); `horizontalDisplayRes`/`verticalDisplayRes` без аргументов; **65 — деление** (`fdivr`, первое на второе). Книга дошла до заставок `zast1`/`zast2` ✅
* 2026-08-17 — 0084 — **Игра доходит до главного меню, кнопки отзываются**: 316 — `sndPlaySound` (звук играет), 411 — переход со зрительным эффектом, 138/`0x4222` — клип (книга уходит по своей запасной ветке), 487 — `TbkTimerStop`. Инструмент `tools/builtins.py`: чем занят любой номер. Покрытие — 59 из 113 номеров, 89 % вызовов ✅
* 2026-08-17 — 0085 — **265 — `ValueNewArray` со сдвинутым кадром** (лишний первый операнд: существующий массив либо пустая ссылка). «Начать новую игру» упирается не в отдельный номер, а в **наборы**: 118 считает по набору, а сборщик 192 в движке заглушка 🟡
* 2026-08-17 — 0086 — **Наборы сделаны**: 192 — сборка (два способа, оба из обёртки RUN67:0x0cfe), 118 — **максимум** (код действия 3 из четырёх: среднее/сумма/минимум/максимум), 165 — дописать с заведением, 166 — `StackPop`, 214 — элемент N с единицы. Индекс 0 у среды — физически последний элемент, потому оба способа сборки дают один порядок. 34/35 отложены осознанно. Барьер сместился на опкод `0x37` — текстовые диапазоны ✅
* 2026-08-19 — 0087 — **Инструменты под измеренный расход контекста**: разбор 14 транскриптов (103 МБ, 6755 ходов) — 939 одноразовых python-скриптов, книга открывалась заново 246 раз, один код дизассемблировался десятками раз. Написаны `patch.py` (дифф вместо полного текста), `pyshell.py` (тёплый питон), `sweep.py` (перебор 0080 с оценкой ложных совпадений), `ne16.py fn/index/xref` (границы функций), `dev.sh`+`rundiff.py` (разница прогонов), `frames.py`+`qmon.py look/await` (кадр словами), `brief.py`→`STATE.md` ✅

## Инструменты
```
  tools/brief.py       Сводка на старт сессии: собирает STATE.md из того, что и так есть.
  tools/builtins.py    Чем занят встроенный номер OpenScript: адрес обёртки и что она зовёт.
  tools/frames.py      Кадры прогона: описание словами, склейка одинаковых, сравнение.
  tools/gdbcli.py      Минимальный клиент удалённого протокола GDB к заглушке QEMU.
  tools/guestprobe.py  Наблюдение за живой средой ToolBook в госте: перехват вызовов и аргументов.
  tools/machopatch.py  Добавляет LC_LOAD_DYLIB в 64-битный Mach-O (thin, little-endian).
  tools/ne16.py        Разбор 16-битного NE: дизассемблер сегмента и разрешение перемещений.
  tools/osc.py         Скрипты OpenScript в книге ToolBook — поиск обработчиков и разбор кода.
  tools/patch.py       Наложение unified diff на файлы репозитория.
  tools/propmap.py     Карта свойств OpenScript: что среда делает с каждым номером.
  tools/pyshell.py     Тёплый питон: одно состояние на много вызовов.
  tools/qmon.py        Управление виртуалкой через монитор QEMU (unix-сокет).
  tools/raw2iso.py     Конвертер raw CD-образов (2352 байта на сектор) в plain ISO 9660 (2048).
  tools/rundiff.py     Что изменилось с прошлого прогона.
  tools/runtime_opcodes.py Inspect the ToolBook 4 OpenScript dispatcher in MTB40RUN.EXE.
  tools/sweep.py       Сплошной перебор с якорями — по правилу из ledger/0080.
  tools/tbk.py         Разборщик книги Asymetrix ToolBook (.TBK) — «Башня знаний».
  tools/tbksrc.py      Исходный текст скриптов, который лежит в книге ToolBook.
  tools/witches.py     Проводник по «Семи ведьмам»: довести игру до нужного места и там оставить.
```

Подробности — `CLAUDE.md` (метод и ловушки стенда) и `NOTES.md` (форматы).
